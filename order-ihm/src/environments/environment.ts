export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:3000',
  enableDebug: true,
  endpoints: {
    orders: '/orders',
    inventory: '/orders'
  
  },
  pagination: {
    defaultPage: 0,
    defaultSize: 10
  }
};