spring:
  datasource:
    # Replace with actual Oracle connection details
    url: jdbc:oracle:thin:@//${BD_ORACLE_HOST:localhost:1521}/XEPDB1
    driverClassName: oracle.jdbc.OracleDriver
    username: ${BD_ORACLE_USER:rachid}
    password: ${BD_ORACLE_USER_PASSWORD:EnovaBeit@1993}
  jpa:
    open-in-view: true
    database-platform: org.hibernate.dialect.Oracle12cDialect
    hibernate:
      ddl-auto: none # Changed from 'validate' to 'none'
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  liquibase:
    enabled: true # Enable Liquibase for this profile
    change-log: classpath:/db/changelog/db.changelog-master.yaml # Path to the master changelog